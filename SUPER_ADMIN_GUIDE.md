# دليل صلاحيات المدير العام (Super Admin)

## نظام الصلاحيات

تم إضافة نظام صلاحيات للمستخدمين يتضمن ثلاثة مستويات:

### 1. 👤 مستخدم عادي (User)
- الصلاحيات: محدودة - استخدام عادي للنظام
- القيمة في قاعدة البيانات: `user`

### 2. 🛡️ مدير (Admin)
- الصلاحيات: إدارية - يمكنه إدارة البيانات
- **لا يمكنه** الوصول إلى التقارير
- القيمة في قاعدة البيانات: `admin`

### 3. 👑 مدير عام (Super Admin)
- الصلاحيات: كاملة - يمكنه الوصول لجميع الميزات
- **يمكنه** الوصول إلى التقارير والإحصائيات
- القيمة في قاعدة البيانات: `super_admin`

## كيفية استخدام Middleware لحماية المسارات

تم إنشاء Middleware خاص بالمدير العام يمكن استخدامه لحماية أي مسار:

### استخدام في الـ Routes

```php
// مثال: حماية صفحة التقارير - متاحة للمدير العام فقط
Route::middleware(['auth', 'super_admin'])->group(function () {
    Route::get('reports', [ReportController::class, 'index'])->name('reports.index');
    Route::get('reports/financial', [ReportController::class, 'financial'])->name('reports.financial');
    Route::get('reports/statistics', [ReportController::class, 'statistics'])->name('reports.statistics');
});
```

### استخدام في الـ Controller

```php
public function __construct()
{
    // حماية جميع دوال الـ Controller
    $this->middleware('super_admin');
}

// أو حماية دوال معينة فقط
public function __construct()
{
    $this->middleware('super_admin')->only(['reports', 'statistics']);
}
```

### التحقق في Blade Templates

```blade
@if(auth()->user()->isSuperAdmin())
    <div class="super-admin-content">
        <!-- محتوى متاح للمدير العام فقط -->
        <a href="{{ route('reports.index') }}">
            <i class="fas fa-chart-bar"></i> التقارير
        </a>
    </div>
@endif

@if(auth()->user()->isAdmin())
    <!-- محتوى متاح للمديرين (بما فيهم المدير العام) -->
@endif
```

### التحقق في الكود

```php
// التحقق من المدير العام
if (auth()->user()->isSuperAdmin()) {
    // كود خاص بالمدير العام
}

// التحقق من المدير (عادي أو عام)
if (auth()->user()->isAdmin()) {
    // كود خاص بالمديرين
}
```

## إضافة مستخدم مدير عام

### من خلال واجهة المستخدمين:
1. انتقل إلى قائمة المستخدمين
2. اضغط على "إضافة مستخدم جديد" أو "تعديل" لمستخدم موجود
3. اختر "مدير عام (Super Admin)" من قسم الصلاحيات
4. احفظ التغييرات

### من خلال قاعدة البيانات:
```sql
UPDATE users SET role = 'super_admin' WHERE email = 'admin@example.com';
```

### من خلال Tinker:
```bash
php artisan tinker
```
```php
$user = User::where('email', 'admin@example.com')->first();
$user->role = 'super_admin';
$user->save();
```

## الواجهات المحدثة

تم تحديث جميع صفحات المستخدمين لتشمل:

✅ **صفحة الإنشاء** - اختيار الصلاحية عند إنشاء مستخدم جديد
✅ **صفحة التعديل** - تعديل صلاحية مستخدم موجود
✅ **صفحة القائمة** - عرض أيقونة وشارة لصلاحية كل مستخدم
✅ **صفحة التفاصيل** - عرض تفصيلي لصلاحية المستخدم

## الأيقونات والألوان

- 👑 **مدير عام**: أيقونة التاج - لون أصفر/ذهبي
- 🛡️ **مدير**: أيقونة الدرع - لون أزرق
- 👤 **مستخدم عادي**: أيقونة المستخدم - لون رمادي

## مثال عملي: إضافة صفحة تقارير

```php
// في routes/web.php
Route::middleware(['auth', 'super_admin'])->group(function () {
    Route::resource('reports', ReportController::class);
});
```

```blade
{{-- في resources/views/layouts/app.blade.php أو القائمة الجانبية --}}
@if(auth()->user()->isSuperAdmin())
    <li class="nav-item">
        <a href="{{ route('reports.index') }}" class="nav-link">
            <i class="nav-icon fas fa-chart-bar"></i>
            <p>التقارير</p>
        </a>
    </li>
@endif
```

## ملاحظات مهمة

⚠️ **تأكد من أن لديك على الأقل مستخدم واحد بصلاحية Super Admin قبل تفعيل حماية المسارات**

⚠️ **لا تحذف جميع المستخدمين ذوي صلاحية Super Admin وإلا لن تتمكن من الوصول للتقارير**

✨ **يمكنك تعديل المستويات حسب احتياجك عن طريق تعديل migration و User model**

## الخطوات القادمة

في المرحلة القادمة سيتم:
- إضافة صفحة التقارير المالية
- إضافة الإحصائيات والرسوم البيانية
- حماية هذه الصفحات باستخدام middleware `super_admin`




